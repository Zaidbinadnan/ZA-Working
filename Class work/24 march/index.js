"use strict";
// template literals
// (backyick) `` shift + below esc
let firstName;
firstName = "Zaid";
let secondName;
secondName = "Adnan";
let fulName = `${firstName} ${secondName}`;
console.log(fulName);
